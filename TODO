* Implement highlevel paradigms (Rpc, Direct, etc.)

* Store frame max length (Use use it corretly)
* Handle heartbeat
* Disable nagle algorithm.
* Allow setting tcp heartbeat

* Clean the amqp_spec interface
