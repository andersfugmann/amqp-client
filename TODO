* Implement highlevel paradigms (Rpc, Direct, etc.)

* Store frame max length (Use it corretly)
* Handle heartbeat
* Allow setting tcp heartbeat
* Clean the amqp_spec interface (Maybe derive the interface and remove spec et. al)
